## Changes

-

## Why

## Testing

- [ ] `cargo fmt --check` / `cargo clippy` / `cargo test` pass locally
- [ ] `npm run lint` / `npm test` / `npm run build` pass locally
      (if frontend changed)
- [ ] Localization resources updated for all four locales (if user-facing
      strings changed)
- [ ] `CHANGELOG.md` updated under "Unreleased" (if user-visible change)

## Screenshots (if UI change)
