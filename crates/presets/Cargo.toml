[package]
name = "laserprep-presets"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Named bundles of conversion/organization parameters per content category (never laser power/speed — see CLAUDE.md Section 12)."

[dependencies]
laserprep-domain.workspace = true
serde.workspace = true
